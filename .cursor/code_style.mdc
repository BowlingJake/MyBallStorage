---
alwaysApply: true
---
## Rule Type: Always

### 程式碼風格指南
- **括號:** 在 `if`/`for`/`while` 等語句後，即使只有一行也要使用花括號 `{}`。
- **命名慣例:**
    - 變數和函數名使用 `camelCase` (小駝峰命名法)。
    - 類別名使用 `PascalCase` (大駝峰命名法)。
    - 常量使用 `SCREAMING_SNAKE_CASE` (全大寫蛇形命名法)。
- **分號:** 在每行語句末尾使用分號。
- **註釋:** 只在複雜或非直觀的程式碼塊前添加註釋，解釋其目的，不對顯而易見的程式碼進行註釋。

### import/require 語句
- 統一使用 ES6 `import` 語句，而不是 `require()`。
- import 語句應該在檔案的頂部，按字母順序排列。

### 錯誤處理
- 優先使用 `try-catch` 塊進行錯誤處理，而不是單純的條件判斷。